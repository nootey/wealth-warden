# Traefik
TRAEFIK_EMAIL=email@email.com
HOST=localhost

# Server Configuration
HOST=0.0.0.0
RELEASE=false

# Traefik Configuration (for Docker deployments)
TRAEFIK_EMAIL=support@wealth.warden

# Finance API Configuration
FINANCE_API_BASE_URL=https://query1.finance.yahoo.com

# HTTP Server Configuration
HTTP_SERVER_PORT=2000
HTTP_SERVER_REQUEST_TIMEOUT=60

# Web Client Configuration
WEB_CLIENT_DOMAIN=localhost
WEB_CLIENT_PORT=5000

# PostgreSQL Configuration
POSTGRES_HOST=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
POSTGRES_DB=wealth_warden

# JWT Configuration (should be set to secure random values)
JWT_WEB_CLIENT_ACCESS=O7yslMel&nR6
JWT_WEB_CLIENT_REFRESH=M2tb,_R!X4w~
JWT_WEB_CLIENT_ENCODE_ID=Rjy6E*)Dz'UwWLPPk*47c0||o`-Oy<p/

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:5000,http://app:5000
# Comma-separated list of wildcard suffixes (optional)
CORS_WILDCARD_SUFFIXES=
# Comma-separated list of allowed schemes
CORS_ALLOWED_SCHEMES=http

# Mailer Configuration
MAILER_HOST=
MAILER_PORT=587
MAILER_USERNAME=
MAILER_PASSWORD=

# Seed Configuration
SEED_SUPER_ADMIN_EMAIL=admin@wealth.warden
SEED_SUPER_ADMIN_PASSWORD=password
SEED_MEMBER_USER_EMAIL=
SEED_MEMBER_USER_PASSWORD=


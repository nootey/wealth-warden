services:
  prometheus:
    volumes:
      - ./config/prometheus/prometheus.local.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

  tempo:
    ports:
      - "3200:3200"
      - "4317:4317"

  loki:
    ports:
      - "3100:3100"

  grafana:
    ports:
      - "3000:3000"

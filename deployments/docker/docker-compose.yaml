services:
  client:
    build:
      context: ../../
      dockerfile: ./build/Dockerfile
      args:
        - API_BASE_URL=app:2000
        - PRODUCTION_MODE=false
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - wealth-warden

networks:
  wealth-warden:
    # external: true

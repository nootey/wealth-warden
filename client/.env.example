# Copy to .env to override the defaults. Both values are optional.

# Port the Vite dev server listens on
VITE_DEV_PORT=5000

# Backend URL the dev server proxies API requests to
VITE_API_PROXY_TARGET=http://localhost:2000
